"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const wasm_1 = require("../wasm");
const util_1 = require("./util");
function listAllAvailablePreviewFeatures(onError) {
    console.log('running preview_features() from prisma-schema-wasm');
    try {
        if (process.env.FORCE_PANIC_PRISMA_SCHEMA_LOCAL) {
            (0, util_1.handleFormatPanic)(() => {
                console.debug('Triggering a Rust panic...');
                wasm_1.prismaSchemaWasm.debug_panic();
            });
        }
        const result = wasm_1.prismaSchemaWasm.preview_features();
        return JSON.parse(result);
    }
    catch (e) {
        const err = e;
        (0, util_1.handleWasmError)(err, 'preview_features', onError);
        return [];
    }
}
exports.default = listAllAvailablePreviewFeatures;
//# sourceMappingURL=listAllAvailablePreviewFeatures.js.map